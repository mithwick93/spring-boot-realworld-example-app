## 1. Persistence

- [x] 1.1 Add `findByReporterId(String userId)` to `ArticleReportRepository` and implement it in `MyBatisArticleReportRepository` / `ArticleReportMapper` (+ `.xml`); verify with a new repository test asserting it returns all reports for a given user across multiple articles, and an empty list for a user with none. **Done (2026-09-03, homework 2.7):** implemented; also added `V3__add_article_report_user_id_index.sql` — a gap found at the post-Plan self-critique gate this homework's WORKFLOW.md requires (design.md's "already indexed" claim didn't hold for a user_id-only query against a composite `(article_id, user_id)` key). 2 new repository tests, both pass.
- [x] 1.2 Post-Plan self-critique gate, per `WORKFLOW.md` §3 (run once, before this task, not deferred to after task 2): found the indexing gap above; fixed with an additive index migration rather than accepted as a known gap, since it was cheap and unambiguous to fix.

## 2. REST controller

- [x] 2.1 Create `CurrentUserReportsApi` at `@RequestMapping(path = "/user/reports")`, `GET` only, `@AuthenticationPrincipal User user`, calling the new repository method; verify with a `@WebMvcTest` asserting the three spec scenarios (multiple reports, empty list, 401 unauthenticated). **Done:** 3 new controller tests, all pass. One thing design.md didn't anticipate, found only at implementation: resolving each report's `articleId` to a slug for the response requires one `articleRepository.findById()` call per report (N+1) — accepted as-is given this endpoint only ever returns one reporter's own (expected-tiny) list, not an admin-scale query; logged in `workflow-breaks-2026-08.md` since neither the design gate nor WORKFLOW.md's rules had a specific check that would have caught this earlier.
- [x] 2.2 Verify no `WebSecurityConfig` change is needed by confirming `/user/reports` is not matched by any existing `permitAll`/method-specific rule (already checked in design.md; re-confirm against the actual file at implementation time in case it has changed since). **Done:** re-read `WebSecurityConfig.java` directly; unchanged since design.md's check, `/user/reports` still falls under `anyRequest().authenticated()`. No config edit needed.

## 3. Verify

- [x] 3.1 Run the full suite (`./gradlew test`) and confirm no regressions against the 82/82 baseline from homework 2.5. **Done:** 87/87 green (82 baseline + 5 new: 2 repository, 3 controller), verified via subagent against a clean copy.
- [x] 3.2 Confirm the existing `POST /articles/{slug}/report` response shape is byte-for-byte unchanged (no `id` field added) — the concrete check for the proposal's "no change to `article-report`" claim. **Done:** `ArticleReportApi.java` and `ArticleReportApiTest.java` untouched by this change (confirmed via `git diff` before commit) — the proposal's "zero modification to `article-report`" claim holds.
