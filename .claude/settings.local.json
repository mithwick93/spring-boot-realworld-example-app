{
  "enabledMcpjsonServers": [
    "idea"
  ],
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Reminder: stage your changes and run /review-changes before committing — it delegates to the read-only reviewer agent, it never edits for you.'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '\"[\\(now|todate)] \\(.tool_name) on \\(.tool_input.file_path // \"n/a\")\"' >> \"$CLAUDE_PROJECT_DIR/.claude-tool-log.txt\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -e '.tool_input.file_path | test(\"application\\\\.properties$\") | not' >/dev/null || { echo 'refused: application.properties is protected (holds jwt.secret + datasource credentials)' >&2; exit 2; }"
          }
        ]
      }
    ]
  },
  "outputStyle": "default"
}
