# Tasks: POST /articles/{slug}/report

Broken out from `plan-2026-08.md`. Each task is sized to be one Claude Code session and one independent commit.

## Task 1 — Persistence layer (this session implements this one, then stops)

Add the ability to durably store a report and have a duplicate rejected at the database level, with no HTTP surface yet.

- `src/main/resources/db/migration/V2__create_article_report_table.sql`
- `src/main/java/io/spring/core/report/ArticleReport.java`
- `src/main/java/io/spring/core/report/ArticleReportRepository.java`
- `src/main/java/io/spring/infrastructure/mybatis/mapper/ArticleReportMapper.java`
- `src/main/resources/mapper/ArticleReportMapper.xml`
- `src/main/java/io/spring/infrastructure/repository/MyBatisArticleReportRepository.java`
- `src/main/java/io/spring/api/exception/DuplicateReportException.java`
- `src/test/java/io/spring/infrastructure/report/MyBatisArticleReportRepositoryTest.java`

**Done when:** a report can be saved and found via the repository test; saving a duplicate `(articleId, userId)` pair throws `DuplicateReportException`; full suite green; commit lands.

**Status (2026-09-02):** implemented and verified — 75/75 tests pass, including the new repository test. Commit is prepared but not yet landed: this sandbox has a stale `.git/index.lock`/`.git/HEAD.lock` (same pre-existing issue documented since 2026-08-28) that blocks git writes from here; the exact `git add`/`git commit` commands have been handed off to run locally.

## Task 2 — REST controller and error wiring

Wire the persistence layer from task 1 up to `POST /articles/{slug}/report`, covering all seven acceptance criteria.

- `src/main/java/io/spring/api/ArticleReportApi.java` (+ `ReportArticleParam` DTO in the same file)
- `src/main/java/io/spring/api/exception/CustomizeExceptionHandler.java` (add the `DuplicateReportException` handler)
- `src/test/java/io/spring/api/ArticleReportApiTest.java`

**Done when:** `ArticleReportApiTest` passes for AC1 (201), AC2 (422 duplicate), AC4 (404 bad slug), AC5 (401 unauthenticated), AC6 (422 blank reason), AC7 (422 over-length reason); full suite green; commit lands.

## Not a task in this list

GraphQL support is explicitly out of scope per `spec-2026-08.md` §5 — no task here adds a `report` mutation.
